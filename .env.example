# WhatSlang Environment Configuration
# Copy this file to .env and fill in your actual values

# ============================================================================
# Application Settings
# ============================================================================
APP_NAME=WhatSlang
DEBUG=false
LOG_LEVEL=INFO

# ============================================================================
# Database Configuration
# ============================================================================
# For local development with SQLite (recommended for testing):
# DATABASE_URL=sqlite+aiosqlite:///./whatslang.db

# For production with PostgreSQL (recommended):
DATABASE_URL=postgresql://whatslang:${DB_PASSWORD}@postgres:5432/whatslang

# Database password (used in docker-compose for PostgreSQL)
DB_PASSWORD=your_secure_database_password_here

# ============================================================================
# WhatsApp API Configuration
# ============================================================================
# Base URL for your go-whatsapp-web-multidevice API instance
WHATSAPP_BASE_URL=https://your-whatsapp-api.example.com

# Optional: Basic auth credentials for WhatsApp API (if required)
WHATSAPP_API_USER=your_username
WHATSAPP_API_PASSWORD=your_password

# ============================================================================
# OpenAI / LLM Configuration
# ============================================================================
# Your OpenAI API key or compatible LLM service key
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Custom LLM endpoint (e.g., LiteLLM, Azure OpenAI)
# Leave empty to use default OpenAI endpoint
OPENAI_BASE_URL=

# Model to use for AI operations
OPENAI_MODEL=gpt-5-mini

# ============================================================================
# Bot Behavior Configuration
# ============================================================================
# How often to poll WhatsApp API for new messages (in seconds)
POLL_INTERVAL_SECONDS=5

# Maximum number of messages to fetch per poll
MESSAGE_LIMIT_PER_POLL=20

# ============================================================================
# Server Configuration
# ============================================================================
# Host to bind the server to (0.0.0.0 for Docker, 127.0.0.1 for local only)
HOST=0.0.0.0

# Port for the backend API
PORT=8000

# Enable auto-reload for development (set to true for local dev)
RELOAD=false

# ============================================================================
# CORS Configuration (Optional)
# ============================================================================
# Comma-separated list of allowed origins for CORS
# Use "*" for development, specific domains for production
# CORS_ORIGINS=*

# ============================================================================
# Frontend Configuration (for Vite development)
# ============================================================================
# Backend API URL (used by frontend in development mode)
# VITE_API_BASE_URL=http://localhost:8000
